// area 

// height = 10;
// width = 20 ;
// area = height * width;
// console.log(area);


// area is the identifier/ name or lable 
// width and height are the received peramiters 
function area(width, height){
   // calculate and then send the results to the call  
    return width * height;
}
// 10 and 20 are the arguments being passed 
console.log(area(10,20)) // call function pass arguments, and receive and print result
console.log(area(12,20))
console.log(area(24,40))


// height = 10
// width = 20
// myArea = area(width, height)
// console.log(myArea)