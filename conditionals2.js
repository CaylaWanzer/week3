// logical operators
// && - and (a;; must be true )
// || - or (either is true )
// == -comparison operator (are the same )
// === -strict comparison (compares both value and datatype)
// != -not equal
// !== -strict
// >= - greater than or equal
// <= - less than or equil to 
// > greater than 
// < - less than 

height = 55
minimum = 60
let canRide = height >= minimum;

console.log("start");

if(canRide){
    console.log("weeeeeeee!");
}else{
    console.log("sadly watching is fun");
}

// console.log("end")